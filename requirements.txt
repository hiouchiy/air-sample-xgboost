# Local / reference dependencies for the XGBoost AI Runtime sample.
# On AI Runtime these are declared in each air/*.yaml (environment.dependencies) instead;
# in a Databricks notebook they are installed by the `%pip` cells at the top of each script.
xgboost>=2.1,<3
scikit-learn>=1.3,<2
mlflow>=2.15.0
databricks-sdk>=0.30.0
